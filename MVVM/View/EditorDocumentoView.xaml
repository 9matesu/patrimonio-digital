<Window x:Class="patrimonio_digital.MVVM.View.EditorDocumentoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:patrimonio_digital.MVVM.View"
        xmlns:vm="clr-namespace:patrimonio_digital.MVVM.ViewModel"
        Title="Editor de Documento"
        Width="500" Height="600"
        WindowStartupLocation="CenterScreen"
        FontFamily="{StaticResource Inter}"
        Icon="/digipat.ico">

    <DockPanel Background="{DynamicResource WindowBackgroundBrush}">
        
        <!-- stackpanel header -->
        <StackPanel Orientation="Vertical" Margin="10" DockPanel.Dock="Top">

            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left">
                <Path Data="M8.56078 20.2501L20.5608 8.25011L15.7501 3.43945L3.75012 15.4395V20.2501H8.56078ZM15.7501 5.56077L18.4395 8.25011L16.5001 10.1895L13.8108 7.50013L15.7501 5.56077ZM12.7501 8.56079L15.4395 11.2501L7.93946 18.7501H5.25012L5.25012 16.0608L12.7501 8.56079Z"
          Fill="{DynamicResource ForegroundBrush}"
          Stretch="Uniform"
          Width="15"
          Height="15"
          Margin="10"/>
                <Label Foreground="White" FontSize="18" VerticalAlignment="Center" Content="Editor de Documento"></Label>

            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" Margin="0 0 0 0">
     
            <Button Content="&lt;" Command="{Binding AnteriorCommand}" Style="{StaticResource EditorButtonTheme}" Margin="2" Width="30"/>
            <Button Content="&gt;" Command="{Binding ProximaCommand}" Style="{StaticResource EditorButtonTheme}" Margin="2" Width="30"/>
            <Button Content="Salvar Tudo" Command="{Binding FecharCommand}" Style="{StaticResource EditorButtonTheme}" Margin="2"/>

                <TextBlock Foreground="White"
                Text="{Binding PaginaAtual.Numero}"
                VerticalAlignment="Center"
                           HorizontalAlignment="Right"
                Margin="10"/>


            </StackPanel>
            
        </StackPanel>


        <TextBox Text="{Binding PaginaAtual.Conteudo, UpdateSourceTrigger=PropertyChanged}"
                     Style="{DynamicResource TextBoxTheme}"
                     AcceptsReturn="True"
                     AcceptsTab="True"
                        VerticalContentAlignment="Top"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     FontSize="14"
                     Margin="10"/>

    </DockPanel>
</Window>
