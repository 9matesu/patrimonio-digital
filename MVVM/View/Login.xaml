<Window x:Class="patrimonio_digital.MVVM.View.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="220" Width="350" WindowStartupLocation="CenterScreen">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Text="Login" 
                   FontSize="20" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,20"/>

        <!-- Nome do usuário -->
        <TextBox x:Name="txtNome"
                 Grid.Row="0"
                 Height="30"
                 Margin="0,0,0,10"
                 />

        <!-- Senha -->
        <PasswordBox x:Name="txtSenha"
                     Grid.Row="1"
                     Height="30"
                     Margin="0,0,0,20"
                     />

        <!-- Botão de login -->
        <Button Content="Entrar"
                Grid.Row="2"
                Width="100"
                Height="30"
                HorizontalAlignment="Center"
                />
    </Grid>
</Window>
